# Somnia Shannon (chain id 50312). Leave DRY_RUN=1 unless you have test STT + collateral.
DRY_RUN=1
SOMNIA_RPC_URL=https://api.infra.testnet.somnia.network
SOMNIA_WS_URL=wss://api.infra.testnet.somnia.network/ws
SOMNIA_INDEXER_URL=https://dev.smk.somnia.host/v1/graphql

# Hex private key for live place/cancel/redeem. Never commit a real key.
# PRIVATE_KEY=0x...

# Optional: seconds before expiry to skip a window (default 30).
# NEAR_EXPIRY_SEC=30
